apiVersion: admissionregistration.k8s.io/v1
kind: MutatingWebhookConfiguration
metadata:
  name: cloudtaser-operator-webhook
  labels:
    app.kubernetes.io/name: cloudtaser-operator
    {{- include "cloudtaser.labels" . | nindent 4 }}
webhooks:
  - name: inject.cloudtaser.io
    admissionReviewVersions: ["v1"]
    sideEffects: None
    timeoutSeconds: {{ .Values.operator.webhook.timeoutSeconds }}
    failurePolicy: {{ .Values.operator.webhook.failurePolicy }}
    reinvocationPolicy: IfNeeded
    clientConfig:
      service:
        name: cloudtaser-operator-webhook
        namespace: {{ include "cloudtaser.namespace" . }}
        path: /mutate-v1-pod
        port: 443
    rules:
      - apiGroups: [""]
        apiVersions: ["v1"]
        operations: ["CREATE"]
        resources: ["pods"]
        scope: Namespaced
    namespaceSelector:
      matchExpressions:
        - key: kubernetes.io/metadata.name
          operator: NotIn
          values:
            - {{ include "cloudtaser.namespace" . }}
            - kube-system
