# Demo-specific overrides only — image repos and tags come from chart defaults.
# This ensures the demo always uses the latest released versions.

operator:
  vaultAddress: "https://secret.cloudtaser.io"

ebpf:
  enabled: true
  enforceMode: true
  reactiveKill: false
