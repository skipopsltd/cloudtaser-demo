# CloudTaser S3 Proxy

Client-side encryption for object storage. Data is encrypted before it leaves your application — the cloud provider stores only ciphertext. Encryption keys stay in EU-hosted Vault, never shared with the provider.

Follow the interactive demo in the terminal →
