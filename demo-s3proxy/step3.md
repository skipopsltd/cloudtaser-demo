# What the Cloud Sees

Bypass the proxy and read the file directly from the cloud. It's ciphertext — the provider can never read your data.

Run in the terminal:

```bash
bash /tmp/step3.sh
```{{exec}}
