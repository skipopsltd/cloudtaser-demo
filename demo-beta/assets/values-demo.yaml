# Demo-specific overrides only — image repos and tags come from chart defaults.

operator:
  ha: true
  vaultAddress: "http://vault.cloudtaser-vault.svc:8200"

ebpf:
  enabled: true
  enforceMode: true
  reactiveKill: false
